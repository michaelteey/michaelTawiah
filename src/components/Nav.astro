---
interface Props {
  current?: 'home' | 'pricing' | 'clients';
}
const { current = 'home' } = Astro.props;
---

<nav class="nav">
  <div class="nav-inner">
    <a href="/" class="logo">mk<span class="dot">.</span></a>
    <div class="nav-sections">
      <a class="nav-anchor" href="/#about">about</a>
      <a class="nav-anchor" href="/#automatedads">platform</a>
      <a class="nav-anchor" href="/#services">services</a>
      <a class="nav-anchor" href="/#method">method</a>
      <a
        class={`nav-page ${current === 'clients' ? 'active' : ''}`}
        href="/clients"
      >clients</a>
      <a
        class={`nav-page ${current === 'pricing' ? 'active' : ''}`}
        href="/pricing"
      >pricing</a>
      <a class="nav-anchor" href="/#skills">skills</a>
    </div>
    <div class="nav-socials-group">
      <a
        href="https://www.linkedin.com/in/michael-tawiah-796255132/"
        class="nav-social"
        target="_blank"
        rel="noopener noreferrer"
      >linkedin ↗</a>
      <a
        href="https://www.instagram.com/michaelkofi_/"
        class="nav-social"
        target="_blank"
        rel="noopener noreferrer"
      >instagram ↗</a>
    </div>
  </div>
</nav>

<style>
  .nav {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 50;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }
  .nav-inner {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }
  .logo {
    font-weight: 700;
    color: var(--fg);
  }
  .logo .dot {
    color: var(--accent);
  }
  .nav-sections,
  .nav-socials-group {
    display: flex;
    gap: 2rem;
    color: var(--muted);
  }
  .nav-socials-group {
    gap: 1.25rem;
  }
  .nav-sections a,
  .nav-socials-group a {
    transition: color 0.2s ease;
  }
  .nav-sections a.active {
    color: var(--accent);
  }
  .nav-social {
    color: var(--accent);
  }

  @media (max-width: 860px) {
    .nav-inner { padding: 1rem 1.25rem; }
    .nav-sections { gap: 1.25rem; }
    .nav-socials-group { gap: 1rem; }
  }
  /* below 520px keep the real pages reachable — only the on-page
     anchors collapse, since they're redundant with scrolling */
  @media (max-width: 520px) {
    .nav-anchor { display: none; }
    .nav-sections { gap: 1rem; font-size: 0.7rem; }
    .nav-socials-group { gap: 0.85rem; font-size: 0.7rem; }
    .nav-inner { padding: 0.85rem 1rem; gap: 0.75rem; }
    .nav-social { letter-spacing: 0.08em; }
  }
  @media (max-width: 400px) {
    .nav-socials-group .nav-social:last-child { display: none; }
  }
</style>
